{
  "name": "nextify-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run --workspaces build",
    "dev": "npm --workspace @nextify/dev-server run dev"
  }
}
